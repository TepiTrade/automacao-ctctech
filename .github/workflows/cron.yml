name: AutoUpdate`non:`n  schedule:`n    - cron: "0 */4 * * *"`n  workflow_dispatch:`njobs:`n  build:`n    runs-on: ubuntu-latest`n    steps:`n      - uses: actions/checkout@v4`n      - name: Update`n        run: |`n          git config user.name "GitHub Action"`n          git config user.email "action@github.com"`n          echo "$(date)" > last_run.txt`n          git add .`n          git commit -m "Cloud Update"`n          git push
